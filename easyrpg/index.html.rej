--- index.html
+++ index.html
@@ -866,7 +864,29 @@
     #viewport {
       position: fixed;
       inset: 0;
-      /* --app-height is kept in sync with the real visible height by JS (see script near </body>).
+      /* --app-height is kept in sync with the real visible height by JS (see script near 
+<script>
+  document.addEventListener('keydown', (e) => {
+    if (e.key === 'Escape') {
+      const modals = [
+        { id: 'image-viewer-modal', closeId: 'image-viewer-close' },
+        { id: 'audio-player-modal', closeId: 'audio-player-close' },
+        { id: 'file-viewer-modal', closeId: 'file-viewer-close' },
+        { id: 'setup-modal', closeId: 'start-btn' },
+        { id: 'download-modal', closeId: 'download-close-btn' }
+      ];
+      for (const m of modals) {
+        const el = document.getElementById(m.id);
+        if (el && window.getComputedStyle(el).display !== 'none') {
+           const btn = document.getElementById(m.closeId);
+           if (btn) btn.click();
+           break;
+        }
+      }
+    }
+  });
+</script>
+</body>).
          100dvh/100vh are just a fallback for the brief moment before that JS runs. */
       height: 100vh;
       height: 100dvh;
@@ -5786,33 +5832,132 @@ class StorageExplorer {
       node.connect(gainNode);
       gainNode.connect(actx.destination);
 
-      await synth.loadSFont(sf2Data.buffer);
-      await synth.addSMFDataToPlayer(data.buffer);
 
-      const modal = document.getElementById('audio-player-modal');
-      const title = document.getElementById('audio-player-title');
-      const audioElem = document.getElementById('audio-player-elem');
-      const midiControls = document.getElementById('midi-player-controls');
-      const seekbar = document.getElementById('midi-player-seekbar');
-      const timeElem = document.getElementById('midi-player-time');
-      const totalElem = document.getElementById('midi-player-total');
-      const volumeBar = document.getElementById('midi-player-volume');
-      
-      if (volumeBar) {
-        volumeBar.value = 100;
-        volumeBar.oninput = () => {
-          gainNode.gain.value = parseInt(volumeBar.value, 10) / 100.0;
-        };
-      }
-      if (timeElem) timeElem.textContent = '00:00';
-      if (totalElem) totalElem.textContent = '00:00';
-      
-      title.textContent = `[MIDI] ${item.name}`;
-      audioElem.style.display = 'none';
-      if (midiControls) midiControls.style.display = 'flex';
-      modal.style.display = 'flex';
+          
+          for (let i = 0; i < numTracks; i++) {
+            if (offset >= view.byteLength) break;
+            const chunkType = view.getUint32(offset);
+            offset += 4;
+            const chunkLength = view.getUint32(offset);
+            offset += 4;
+            if (chunkType === 0x4D54726B) {
+              let trackOffset = { val: offset };
+              const trackEnd = offset + chunkLength;
+              let absoluteTick = 0, lastStatus = 0;
+              while (trackOffset.val < trackEnd) {
+                const delta = readVarInt(view, trackOffset);
+                absoluteTick += delta;
+                if (trackOffset.val >= trackEnd) break;
+                let status = view.getUint8(trackOffset.val);
+                if (status < 0x80) status = lastStatus;
+                else { lastStatus = status; trackOffset.val++; }
+                const type = status & 0xF0;
+                if (status === 0xFF) {
+                  const metaType = view.getUint8(trackOffset.val++);
+                  const metaLen = readVarInt(view, trackOffset);
+                  if (metaType === 0x51 && metaLen === 3) {
+                    const b1 = view.getUint8(trackOffset.val);
+                    const b2 = view.getUint8(trackOffset.val + 1);
+                    const b3 = view.getUint8(trackOffset.val + 2);
+                    tempoChanges.push({ tick: absoluteTick, usPerQuarter: (b1 << 16) | (b2 << 8) | b3 });
+                  }
+                  trackOffset.val += metaLen;
+                } else if (status === 0xF0 || status === 0xF7) {
+                  const sysLen = readVarInt(view, trackOffset);
+                  trackOffset.val += sysLen;
+                } else {
+                  if (type === 0xB0) {
+                     const cc = view.getUint8(trackOffset.val);
+                     if (cc === 111 && (loopStart === -1 || absoluteTick < loopStart)) {
+                         loopStart = absoluteTick;
+                     }
+                  }
+                  if (type === 0xC0 || type === 0xD0) trackOffset.val += 1;
+                  else trackOffset.val += 2;
+                }
+              }
+              if (absoluteTick > maxTick) maxTick = absoluteTick;
+            }
+            offset += chunkLength;
+          }
+          tempoChanges.sort((a, b) => a.tick - b.tick);
+          return { maxTick, tempoChanges, ppqn, loopStart };
+        } catch (e) {
+          console.warn("MIDI parse error", e);
+          return null;
+        }
+      };
+
+          const writeVarInt = (val) => {
+              let buf = []; val = Math.floor(val); buf.push(val & 0x7F); val >>= 7;
+              while (val > 0) { buf.unshift((val & 0x7F) | 0x80); val >>= 7; }
+              return buf;
+          };
+          const tracks = [];
+          for (let i = 0; i < numTracks; i++) {
+            if (offset >= view.byteLength) break;
+            const chunkType = view.getUint32(offset); offset += 4;
+            const chunkLength = view.getUint32(offset); offset += 4;
+            if (chunkType === 0x4D54726B) {
+              let ptr = { val: offset }, tEnd = offset + chunkLength, absTick = 0, lastSt = 0, evs = [];
+              try {
+                while (ptr.val < tEnd) {
+                  const delta = readVarInt(view, ptr); absTick += delta;
+                  if (ptr.val >= tEnd) break;
+                  const eStart = ptr.val; let st = view.getUint8(ptr.val), run = false;
+                  if (st < 0x80) { st = lastSt; run = true; } else { lastSt = st; ptr.val++; }
+                  const ty = st & 0xF0;
+                  if (st === 0xFF) { ptr.val++; ptr.val += readVarInt(view, ptr); }
+                  else if (st === 0xF0 || st === 0xF7) ptr.val += readVarInt(view, ptr);
+                  else ptr.val += (ty === 0xC0 || ty === 0xD0) ? 1 : 2;
+                  let dLen = ptr.val - eStart, eData;
+                  if (run) { eData = new Uint8Array(dLen + 1); eData[0] = st; for(let j=0; j<dLen; j++) eData[j+1] = view.getUint8(eStart + j); }
+                  else { eData = new Uint8Array(dLen); for(let j=0; j<dLen; j++) eData[j] = view.getUint8(eStart + j); }
+                  evs.push({ t: absTick, d: eData, eot: (st === 0xFF && eData[1] === 0x2F) });
+                }
+              } catch(e) { console.warn("Track parse err", e); }
+              tracks.push(evs);
+            }
+            offset += chunkLength;
+          }
+          const outTrks = [], lLen = maxTick - loopStart;
+          for (let evs of tracks) {
+              const ne = [], pr = [], ls = [], po = [];
+              for (let e of evs) {
+                  if (e.eot) po.push(e); else if (e.t < loopStart) pr.push(e); else if (e.t < maxTick) ls.push(e);
+              }
+              ne.push(...pr);
+              for (let c = 0; c < loopCount; c++) {
+                  const off = c * lLen;
+                  for (let e of ls) ne.push({ t: e.t + off, d: e.d, eot: false });
+              }
+              const fOff = loopCount > 0 ? (loopCount - 1) * lLen : 0;
+              for (let e of po) ne.push({ t: Math.max(e.t, maxTick) + fOff, d: e.d, eot: true });
+              ne.sort((a, b) => a.t - b.t); outTrks.push(ne);
+          }
+          let totSz = 14; const trkBufs = [];
+          for (let ne of outTrks) {
+              const td = []; let lt = 0;
+              for (let e of ne) {
+                  const dBytes = writeVarInt(e.t - lt);
+                  for (let b of dBytes) td.push(b);
+                  for (let b of e.d) td.push(b);
+                  lt = e.t;
+              }
+              trkBufs.push(td); totSz += 8 + td.length;
+          }
+          const out = new Uint8Array(totSz), ov = new DataView(out.buffer);
+          ov.setUint32(0, 0x4D546864); ov.setUint32(4, 6); ov.setUint16(8, format);
+          ov.setUint16(10, outTrks.length); ov.setUint16(12, division);
+          let p = 14;
+          for (let td of trkBufs) {
+              ov.setUint32(p, 0x4D54726B); p += 4; ov.setUint32(p, td.length); p += 4;
+              for (let b of td) out[p++] = b;
+          }
+          return out;
+        } catch(e) { console.warn("Unroll error", e); return buffer; }
+      };
 
-      await synth.playPlayer();
 
       const formatTime = (seconds) => {
          if (isNaN(seconds) || !isFinite(seconds) || seconds < 0) return "00:00";
@@ -6015,11 +6254,45 @@ class StorageExplorer {
         return time;
       };
 
-      const parsedMidi = parseMidiForTime(data.buffer);
-      const totalExactSeconds = parsedMidi ? calculateTimeAtTick(parsedMidi.maxTick, parsedMidi) : 0;
-      if (totalElem && totalExactSeconds > 0) {
-         totalElem.textContent = formatTime(totalExactSeconds);
+      const parsedMidi = parseMidiForTime(data);
+      console.log("[MIDI PARSER] Result:", parsedMidi);
+      
+      let finalData = data;
+      if (parsedMidi && parsedMidi.loopStart !== -1) {
+          finalData = unrollMidi(data, parsedMidi.loopStart, parsedMidi.maxTick, 10);
+      }
+
+      await synth.loadSFont(sf2Data.buffer);
+      await synth.addSMFDataToPlayer(finalData.buffer);
+
+      const modal = document.getElementById('audio-player-modal');
+      const title = document.getElementById('audio-player-title');
+      const audioElem = document.getElementById('audio-player-elem');
+      const midiControls = document.getElementById('midi-player-controls');
+      const seekbar = document.getElementById('midi-player-seekbar');
+      const timeElem = document.getElementById('midi-player-time');
+      const totalElem = document.getElementById('midi-player-total');
+      const volumeBar = document.getElementById('midi-player-volume');
+      const midiVolume = document.getElementById('midi-volume-container');
+      const loopCheck = document.getElementById('audio-player-loop');
+      
+      if (volumeBar) {
+        volumeBar.value = 100;
+        volumeBar.oninput = () => {
+          gainNode.gain.value = parseInt(volumeBar.value, 10) / 100.0;
+        };
       }
+      if (timeElem) timeElem.textContent = '00:00';
+      if (totalElem) totalElem.textContent = '00:00';
+      
+      title.textContent = `[MIDI] ${item.name}`;
+      audioElem.style.display = 'none';
+      if (midiControls) midiControls.style.display = 'flex';
+      if (midiVolume) midiVolume.style.display = 'flex';
+      modal.style.display = 'flex';
+
+      await synth.playPlayer();
+
 
       let updateInterval = null;
       if (seekbar) {
